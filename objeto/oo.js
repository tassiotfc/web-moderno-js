// CÓDIGO NÃO EXECUTÁVEL

// Procedural: foco em funções que manipulavam os dados
processamento(valor1, valor2, valor3)

// OO: foco em dados que dentro tem funções que manipulam os dados
objeto = {
    valor1,
    valor2,
    valor3,
    processamento(){
        //...
    }
}

objeto.processamento() //  Foco no objeto

/** 
 * Principios OO
 * 1. Abstração: mapear o mundo real pro mundo de sistemas de acordo com o contexto
 * 2. Encapsulamento: esconder detalhes de implementação e mostrar interface simples para quem usa
 * 3. Herança: baseado na relação do tipo: é 1
 * 4. Polimorfismo: multiplas formas
 */